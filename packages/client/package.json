{
  "name": "@xiyou/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "wrangler pages deploy dist --project-name=xiyou-roguelike",
    "deploy:preview": "wrangler pages deploy dist --project-name=xiyou-roguelike --branch=preview"
  },
  "dependencies": {
    "phaser": "^3.80.1",
    "@xiyou/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.1.0",
    "wrangler": "^3.100.0"
  }
}
